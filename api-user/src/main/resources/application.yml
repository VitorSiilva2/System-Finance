spring:
  application:
    name: USER-API
server:
  port: ${PORT:8100}
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
api:
  security:
    token:
      secret: ${JWT_SECRET:my-secret-key}
